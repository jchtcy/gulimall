# 一、环境搭建
````
1、添加hosts内容 192.168.48.129 item.gulimall.com
2、修改网关 使item路由到product
3、复制item.html 到 product模块的 src/main/resources/templates下
4、复制 resource/静态资源/html/static/item 到 /mydata/nginx/html/static
````
# 二、查询商品详情
````
1、新建 SkuItemVo
2、查询当前商品详情 /{skuId}.html
3、此时启动报错, 因为复制别人页面是完整功能, 先用postman测试
````
# 三、异步编排
````
1、product模块引入Properties提示工具
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-configuration-processor</artifactId>
    <optional>true</optional>
</dependency>


2、写线程池自定义配置 ThreadPoolConfigProperties

3、在application.yml里编写线程池配置

4、写线程池配置类 MyThreadConfig

5、修改SkuInfoService.item 改成异步任务
````
